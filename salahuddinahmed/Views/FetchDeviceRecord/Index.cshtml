@model IEnumerable<BioMetrixCore.MachineInfo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MachineNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IndRegID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTimeRecord)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOnlyRecord)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TimeOnlyRecord)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MachineNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IndRegID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTimeRecord)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOnlyRecord)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeOnlyRecord)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
